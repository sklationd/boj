#include <iostream>
#define fastio ios_base :: sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

int main(){
    // for fast io 
    fastio;
	int a,b;
	cin >> a >> b;
	cout << 2*b - a << endl;
}
